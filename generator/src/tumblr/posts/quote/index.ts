import { renderBlock, renderString } from '~/tumblr/helpers'

export const IfSource = renderBlock('Source')

export const Length = renderString('Length')
export const Quote = renderString('Quote')
export const Source = renderString('Source')
